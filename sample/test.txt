/*int add(int x, int y, int z);
*/

#include <stdio.h>

int gl;
//in a;
int add(int x, int y){
    int ret;
    int res;
    ret = x + y;
    return ret;
}

int mul(int x, int y, int z) {
    int ret;
    //int ret;
    //int res;
    ret = x + y + z;
    ret = x * y + y / (z + x);
    return ret;
}

int main() {
    int a;
    int b[5];
    //a = mul(1,2,1);
    if (!(a > 0) && (a < 5)) {
        a = (a + 1) * 5;
        b[2] = b[1] / 2;
    } else {
        if (b[0] > 9 || a != 0) {
            a = a & b[0] + add(1, 5);
        } else {
            a = a | b[2];
        }
    }
    return 0;
}

/*int main(){
    int a;
    //int b;
    a = a + b;
}*/